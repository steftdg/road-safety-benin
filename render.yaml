services:
  - type: web
    name: road-safety-benin
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0